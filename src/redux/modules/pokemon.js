const initialState = [];

const pokeSelect = (state = initialState, action) => {
  switch (action.type) {
    // type:"Remove_Pokemon":

    default:
      return state;
  }
};

export default pokeSelect;
